const config = {
    // 启动端口
    port: 3000,
    // 数据库配置
    database: {
        DATABASE: 'library',
        USERNAME: 'root',
        PASSWORD: 'fuzhi001',
        PORT: '3306',
        HOST: '192.168.60.20'
    }
}

module.exports = config